
  # mouth freshner

  This is a code bundle for mouth freshner. The original project is available at https://www.figma.com/design/OkX5cMFzSwhLF7KLeSpDcv/mouth-freshner.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  